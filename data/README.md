Place your telephony CSV export here as `calls.csv`.

Expected columns include at minimum: `Agent`, call date, talk time, recording URL, and disposition fields. See `PIPELINE_RUNBOOK.md` for column mapping details.

This folder is gitignored — never commit real call data.
